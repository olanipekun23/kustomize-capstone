apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-webapp
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: webapp
        env:
        - name: ENV
          value: "production"

